@model IEnumerable<BTLPMQL.Models.PhanHoi>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenKhachHang)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ykien)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenKhachHang)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ykien)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDkhachhang }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDkhachhang }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDkhachhang })
        </td>
    </tr>
}

</table>
